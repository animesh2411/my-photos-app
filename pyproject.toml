[project]
name = "my-photos-app"
version = "1.1.2"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "pillow>=12.3.0",
    "uvicorn>=0.50.0",
]

[tool.pytest.ini_options]
pythonpath = ["backend"]
